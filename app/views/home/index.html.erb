<!DOCTYPE html>
<html>
<head>
  <title>GeocodingApp - Your Location Intelligence Solution</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>

<body>
  <div class="hero-section">
    <div class="content-wrapper">
      <div class="text-content">
        <div class="app-name">GeocodingApp</div>
        <h1 class="title">Transform Addresses into Location Intelligence</h1>
        <p class="description">
          Unlock the power of location data with our advanced geocoding solution. 
          Convert addresses into precise coordinates and enhance your spatial analysis capabilities.
        </p>
        <div class="cta-buttons">
          <% if user_signed_in? %>
            <%= link_to "My Profile", profile_path, class: "cta-primary" %>
            <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "cta-secondary", form_class: "inline-form" %>
          <% else %>
            <%= link_to "Sign Up", new_user_registration_path, class: "cta-primary" %>
            <%= link_to "Log In", new_user_session_path, class: "cta-secondary" %>
          <% end %>
        </div>
      </div>
      
      <div class="image-content">
        <div class="svg-container">
          <%= image_tag "location_pin.svg", alt: "Location Intelligence Illustration", width: "100%" %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
